import React from "react";
import "./Header.css";

const Navbar = () => (
    // console.log()
    <div className="header">
        <h1>Game of Clicks</h1>
        <h2>Click an image to live, but do not click the same one twice!</h2>
    </div>
)

export default Navbar;