import React from "react";
import "./Header.css";

const Navbar = () => (
    <div className="header">
        <p id="subhead">Click an image to live, but do not click the same one twice!</p>
    </div>
)

export default Navbar;